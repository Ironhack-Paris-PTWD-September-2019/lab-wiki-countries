import React from "react";

const Home = (props) => {
    return (
        <React.Fragment>
            <h1>Welcome</h1>
            <p>Let's find out more about countries: select one country to get details.</p>
        </React.Fragment>
    )
}

export default Home;